<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:OutWit.Common.Controls.Menu"
                    xmlns:output="clr-namespace:OutWit.Common.Controls.Output">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/OutWit.Common.Controls;component/Resources.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--<Style TargetType="local:ButtonMenuItem" BasedOn="{StaticResource MaterialDesignMenuItem}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Header">
            <Setter.Value>
                <Grid>
                    <output:IconOutput Width="{Binding Path=Width, RelativeSource={RelativeSource AncestorType={x:Type local:ButtonMenuItem}}}"
                                       Text="{Binding Path=Text, RelativeSource={RelativeSource AncestorType={x:Type local:ButtonMenuItem}}}"
                                       Kind="{Binding Path=Kind, RelativeSource={RelativeSource AncestorType={x:Type local:ButtonMenuItem}}}"
                                       IconPosition="{Binding Path=IconPosition, RelativeSource={RelativeSource AncestorType={x:Type local:ButtonMenuItem}}}"/>
                </Grid>
            </Setter.Value>
        </Setter>
    </Style>-->

</ResourceDictionary>